# Pearls AQI Predictor - runtime + dashboard dependencies (LEAN).
#
# Streamlit Cloud installs THIS file, so it is kept light and wheel-only:
# no TensorFlow / XGBoost / LightGBM / SHAP. That makes the deployed dashboard
# install fast and avoids the "unsatisfiable" / source-build failures seen on
# Streamlit Cloud's newer Python.
#
# For the FULL local training stack (deep learning + boosting + SHAP) use:
#     pip install -r requirements-full.txt   (on Python 3.11 or 3.12)

# ---- Core data + ML ----
pandas>=2.1.0,<2.3.0
numpy>=1.26.0,<2.0.0
scikit-learn>=1.4.0,<1.7.0
joblib>=1.3.0

# ---- Data sources + serverless feature store / model registry ----
requests>=2.31.0
hopsworks>=4.7.0,<4.8.0
confluent-kafka>=2.6.0

# ---- Dashboard + API ----
streamlit>=1.33.0
plotly>=5.20.0
flask>=3.0.0

# ---- Config / utils / IO / EDA charts ----
pyyaml>=6.0
python-dotenv>=1.0.0
pyarrow>=15.0.0,<19.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
