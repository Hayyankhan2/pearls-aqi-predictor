# Pearls AQI Predictor - dependencies
# Install: pip install -r requirements.txt

# ---- Core data + ML ----
pandas>=2.1.0,<2.3.0
numpy>=1.26.0,<2.0.0
scikit-learn>=1.4.0,<1.7.0
joblib>=1.3.0

# ---- Gradient boosting (optional but recommended) ----
xgboost>=2.0.0
lightgbm>=4.3.0

# ---- Deep learning (optional; ensemble works without it) ----
tensorflow-cpu>=2.15.0 ; platform_system != "Darwin"
tensorflow>=2.15.0 ; platform_system == "Darwin"

# ---- Explainability ----
shap>=0.44.0

# ---- Serverless feature store + model registry ----
# Optional: the project falls back to a local store if this is not installed.
hopsworks>=4.8.0,<5.0.0

# ---- Data sources ----
requests>=2.31.0

# ---- Dashboard ----
streamlit>=1.33.0
plotly>=5.20.0
flask>=3.0.0

# ---- Config / utils ----
pyyaml>=6.0
python-dotenv>=1.0.0
pyarrow>=15.0.0,<19.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
