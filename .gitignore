# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Secrets
.env

# Data / artifacts (sample data is committed explicitly with -f if needed)
data/*.parquet
data/*.csv
data/*.json
raw_data/
figures/*.png

# Models (registered artifacts are regenerated by the training pipeline)
models/*.joblib
models/*.keras
models/*.h5
models/*.json
models/*.csv

# Keep folder placeholders
!data/.gitkeep
!models/.gitkeep
!figures/.gitkeep

# OS / IDE
.DS_Store
.ipynb_checkpoints/
.vscode/
.idea/
